# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# npmrc-replace-env

Generates `.npmrc` files from a `.npmrc.config` template by replacing env var placeholders via `dotenv-flow`.
