/**
 * The default name of the .npmrc file.
 */
export const DEFAULT_NPMRC_FILE = ".npmrc" as const;

/**
 * The default configuration file name.
 */
export const DEFAULT_CONFIG_FILE = ".npmrc.config" as const;

/**
 * The default environment variable prefix used for replacing values in .npmrc files.
 */
export const DEFAULT_ENV_PREFIX = "NPMRC_" as const;
